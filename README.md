# spack-envs
This repository contains the spack.yaml to define the specs of tools, utils, assemblers and advanced gcc compilers packages to be installed with system gcc and to define the external system installations 
