# spack-envs
This repository contains the spack.yaml that defines the specs of tools, utils, assemblers and compilers packages to be installed with system, cuda13, icelake target and to define the external system installations.
